# TaskFlow - E2Eテスト設計書

> Generated by CCAGI SDK Phase 2 | CodeGenAgent (Gen)
> Date: 2026-02-27
> Version: 1.0.0
> Source: docs/design/spec.md, docs/requirements/requirements.md

---

## 1. 概要

### 1.1 目的

ユーザーストーリー（US-001〜US-010）をベースに、ブラウザ上でのエンドツーエンド操作を検証する。ユーザーの視点でアプリケーション全体が正しく機能することを保証する。

### 1.2 テストフレームワーク

| 項目 | 技術 |
|------|------|
| E2Eフレームワーク | Playwright |
| ブラウザ | Chromium, Firefox, WebKit |
| テスト環境 | Docker Compose (Next.js + PostgreSQL) |
| スクリーンショット比較 | Playwright built-in screenshot comparison |
| ビデオ録画 | Playwright built-in video recording |

### 1.3 テスト環境

```yaml
# docker-compose.e2e.yml
services:
  app:
    build: .
    environment:
      DATABASE_URL: postgresql://test:test@postgres:5432/taskflow_e2e
      NODE_ENV: test
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: taskflow_e2e
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test"]
      interval: 5s
      retries: 5
```

### 1.4 テストファイル配置

```
tests/
  e2e/
    fixtures/
      auth.ts            # 認証フィクスチャ
      project.ts         # プロジェクトフィクスチャ
      task.ts            # タスクフィクスチャ
    pages/
      LoginPage.ts       # ログインページオブジェクト
      DashboardPage.ts   # ダッシュボードページオブジェクト
      ProjectPage.ts     # プロジェクトページオブジェクト
      GanttPage.ts       # ガントチャートページオブジェクト
      KanbanPage.ts      # カンバンページオブジェクト
      SettingsPage.ts    # 設定ページオブジェクト
    scenarios/
      us001-task-creation.spec.ts
      us002-gantt-chart.spec.ts
      us003-dependencies.spec.ts
      us004-drag-and-drop.spec.ts
      us005-progress-update.spec.ts
      us006-kanban-board.spec.ts
      us007-notifications.spec.ts
      us009-filtering-search.spec.ts
      us010-dark-mode.spec.ts
    screenshots/
      baseline/          # ベースラインスクリーンショット
```

### 1.5 Playwright設定

```typescript
// playwright.config.ts
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  fullyParallel: false,  // シナリオ間のデータ依存を考慮
  retries: 1,
  workers: 1,
  reporter: [
    ["html", { outputFolder: ".test-logs/latest/e2e-report" }],
    ["json", { outputFile: ".test-logs/latest/tests/e2e-results.json" }],
  ],
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
    screenshot: "only-on-failure",
    video: "retain-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
    {
      name: "firefox",
      use: { ...devices["Desktop Firefox"] },
    },
    {
      name: "webkit",
      use: { ...devices["Desktop Safari"] },
    },
    {
      name: "mobile-chrome",
      use: { ...devices["Pixel 7"] },
    },
    {
      name: "mobile-safari",
      use: { ...devices["iPhone 14"] },
    },
  ],
});
```

---

## 2. ページオブジェクトモデル

### 2.1 LoginPage

```typescript
// tests/e2e/pages/LoginPage.ts
export class LoginPage {
  constructor(private page: Page) {}

  async goto() { await this.page.goto("/login"); }
  async clickDemoLogin() { await this.page.click('[data-testid="demo-login-btn"]'); }
  async waitForRedirect() { await this.page.waitForURL("/"); }
  async isLoginPage() { return this.page.url().includes("/login"); }
}
```

### 2.2 DashboardPage

```typescript
// tests/e2e/pages/DashboardPage.ts
export class DashboardPage {
  constructor(private page: Page) {}

  async goto() { await this.page.goto("/"); }
  async clickNewProject() { await this.page.click('[data-testid="new-project-btn"]'); }
  async fillProjectName(name: string) { /* ... */ }
  async submitProject() { /* ... */ }
  async getProjectCards() { return this.page.locator('[data-testid="project-card"]').all(); }
  async clickProject(name: string) { /* ... */ }
}
```

### 2.3 ProjectPage / GanttPage / KanbanPage

```typescript
// tests/e2e/pages/ProjectPage.ts
export class ProjectPage {
  constructor(private page: Page) {}

  async clickAddTask() { await this.page.click('[data-testid="add-task-btn"]'); }
  async fillTaskTitle(title: string) { /* ... */ }
  async submitTask() { /* ... */ }
  async switchToGantt() { await this.page.click('[data-testid="tab-gantt"]'); }
  async switchToKanban() { await this.page.click('[data-testid="tab-kanban"]'); }
  async switchToCalendar() { await this.page.click('[data-testid="tab-calendar"]'); }
  async switchToList() { await this.page.click('[data-testid="tab-list"]'); }
}
```

---

## 3. テストシナリオ

### 3.1 US-001: タスク作成フロー

**ファイル:** `us001-task-creation.spec.ts`
**対応ユーザーストーリー:** US-001 (タスク作成)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-001 | タイトルのみでタスク作成 | ログイン済み、プロジェクト存在 | 1. 「+ Add Task」クリック 2. タイトル入力 3. 送信 | タスクがリストに表示される |
| E2E-002 | 全フィールド指定でタスク作成 | ログイン済み、プロジェクト存在 | 1. 「+ Add Task」クリック 2. タイトル、説明、開始日、終了日、優先度を入力 3. 送信 | 全フィールドが反映されたタスクが表示 |
| E2E-003 | 開始日・終了日をカレンダーピッカーで設定 | ログイン済み、プロジェクト存在 | 1. タスク作成ダイアログ 2. カレンダーピッカーで日付選択 | 選択した日付が設定される |
| E2E-004 | 優先度を4段階から選択 | ログイン済み、プロジェクト存在 | 1. タスク作成ダイアログ 2. 各優先度を選択 | Critical/High/Medium/Low が選択可能 |
| E2E-005 | 作成後にガントチャートに即時反映 | ログイン済み、ガントチャート表示中 | 1. タスク作成 2. ガントチャート確認 | 新タスクのバーが表示される |
| E2E-006 | タイトル空でバリデーションエラー | ログイン済み | 1. 「+ Add Task」クリック 2. タイトル空で送信 | エラーメッセージが表示される |
| E2E-007 | タスク作成ダイアログのキャンセル | ログイン済み | 1. 「+ Add Task」クリック 2. キャンセル | ダイアログが閉じ、タスクは作成されない |

---

### 3.2 US-002: ガントチャート表示・操作

**ファイル:** `us002-gantt-chart.spec.ts`
**対応ユーザーストーリー:** US-002 (ガントチャート俯瞰)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-010 | ガントチャートにタスクバー表示 | 複数タスク存在 | 1. プロジェクト画面 2. ガントタブ選択 | 全タスクがバーとして表示 |
| E2E-011 | 日/週/月ビュー切替 | ガントチャート表示中 | 1. Day ボタンクリック 2. Week ボタンクリック 3. Month ボタンクリック | 各スケールで表示が変わる |
| E2E-012 | 今日線が赤い点線で表示 | ガントチャート表示中 | 1. ガントチャート確認 | 赤い点線の今日線が表示される |
| E2E-013 | タスクバーの進捗表示 | 進捗率50%のタスク存在 | 1. ガントチャート確認 | バーに進捗の塗りつぶしが表示 |
| E2E-014 | マイルストーンのダイヤモンド表示 | マイルストーン存在 | 1. ガントチャート確認 | ダイヤモンド形状のマイルストーンが表示 |
| E2E-015 | タスクバークリックで詳細Sheet表示 | ガントチャート表示中 | 1. タスクバークリック | 右側にタスク詳細Sheetが表示 |
| E2E-016 | ビュー切替の状態維持 | ガントチャート表示中 | 1. 月ビュー選択 2. ページ内操作 3. ビュー確認 | 月ビューが維持される |
| E2E-017 | 100タスクでの描画パフォーマンス | 100タスク存在 | 1. ガントチャート表示 2. 描画時間計測 | 500ms以内に描画完了 |

---

### 3.3 US-003: 依存関係設定

**ファイル:** `us003-dependencies.spec.ts`
**対応ユーザーストーリー:** US-003 (依存関係とクリティカルパス)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-020 | FS依存関係の設定 | 2つ以上のタスク存在 | 1. タスク詳細Sheet 2. 「+ Add Dependency」クリック 3. 先行タスク選択 4. FS選択 | 依存関係が追加される |
| E2E-021 | 依存関係矢印の表示 | FS依存関係設定済み | 1. ガントチャート確認 | 先行タスクから後続タスクへの矢印が表示 |
| E2E-022 | SS/FF/SF依存関係の設定 | 2つ以上のタスク存在 | 1. 各タイプで依存関係を設定 | 正しいタイプで設定される |
| E2E-023 | 循環依存のエラー表示 | A->B->C の依存存在 | 1. C->A の依存関係を設定しようとする | エラーメッセージが表示され設定不可 |
| E2E-024 | 依存関係の削除 | 依存関係設定済み | 1. タスク詳細Sheet 2. 依存関係の「Remove」クリック | 依存関係が削除され矢印が消える |

---

### 3.4 US-004: ドラッグ&ドロップ操作

**ファイル:** `us004-drag-and-drop.spec.ts`
**対応ユーザーストーリー:** US-004 (ドラッグ&ドロップ)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-030 | ガントバーのドラッグで日程変更 | タスク存在 | 1. ガントバー中央をドラッグ 2. 右に3日分ドロップ | startDate/endDateが3日後に更新 |
| E2E-031 | ガントバー右端のリサイズで期間変更 | タスク存在 | 1. ガントバー右端をドラッグ 2. 右に2日分ドロップ | endDateが2日後に更新 |
| E2E-032 | ドラッグ中のゴーストプレビュー表示 | タスク存在 | 1. ガントバードラッグ開始 | ゴースト（半透明）プレビューが表示 |
| E2E-033 | ドラッグ後のAPI更新 | タスク存在 | 1. ガントバーをドラッグ&ドロップ 2. ページリロード | 更新された日付がDBに永続化 |
| E2E-034 | カンバンカードのカラム間ドラッグ | タスク存在 | 1. TODOカードをIN_PROGRESSカラムへドラッグ | ステータスがIN_PROGRESSに変更 |
| E2E-035 | カンバンカードのカラム内ドラッグ | 同カラムに2タスク以上 | 1. カード順序を入れ替え | sortOrderが更新される |
| E2E-036 | 依存関係ありタスクのドラッグ警告 | 依存関係設定済み | 1. 後続タスクを先行タスク完了前にドラッグ | 警告が表示される |

---

### 3.5 US-005: 進捗率更新

**ファイル:** `us005-progress-update.spec.ts`
**対応ユーザーストーリー:** US-005 (進捗率更新)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-040 | スライダーで進捗率を50%に設定 | タスク詳細Sheet表示中 | 1. スライダーを50%位置へドラッグ | 進捗率が50%に更新、バーの塗りつぶしが50% |
| E2E-041 | 進捗率100%でステータスがDONEに自動変更 | タスク詳細Sheet表示中 (TODO) | 1. スライダーを100%へ | status === "DONE" |
| E2E-042 | 進捗率0%でステータスがTODOに自動変更 | タスク詳細Sheet表示中 (DONE) | 1. スライダーを0%へ | status === "TODO" |
| E2E-043 | 進捗率1-99%でステータスがIN_PROGRESSに自動変更 | タスク詳細Sheet表示中 (TODO) | 1. スライダーを30%へ | status === "IN_PROGRESS" |
| E2E-044 | ガントバーの進捗塗りつぶしがリアルタイム反映 | ガントチャート + Sheet表示 | 1. Sheetでスライダー操作 2. ガントバー確認 | バー内の塗りつぶし幅が変化 |
| E2E-045 | 進捗率がページリロード後も保持 | 進捗率更新済み | 1. ページリロード | 更新した進捗率が表示される |

---

### 3.6 US-006: カンバンボード操作

**ファイル:** `us006-kanban-board.spec.ts`
**対応ユーザーストーリー:** US-006 (カンバンボード)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-050 | 4つのステータスカラムが表示 | カンバンビュー表示 | 1. カンバンタブ選択 | TODO, IN_PROGRESS, DONE, ON_HOLD カラム表示 |
| E2E-051 | 各カラムにタスク数バッジ表示 | 各ステータスにタスク存在 | 1. カンバン確認 | 各カラムヘッダーにタスク数表示 |
| E2E-052 | カードにタスク情報表示 | タスク存在 | 1. カンバン確認 | カードにタイトル、優先度、期限、進捗率表示 |
| E2E-053 | カラム間ドラッグでステータス変更 | TODOタスク存在 | 1. TODOカードをDONEにドラッグ | ステータスがDONEに更新 |
| E2E-054 | カラム間移動後のカウント更新 | 移動前のカウント確認 | 1. カード移動 | 移動元-1、移動先+1 |
| E2E-055 | カードクリックで詳細Sheet表示 | タスク存在 | 1. カードクリック | タスク詳細Sheetが表示 |
| E2E-056 | Sheet内でステータス変更→カンバン反映 | Sheet表示中 | 1. Sheetでステータス変更 2. Sheet閉じる | カードが新しいカラムに移動 |

---

### 3.7 US-007: 通知確認

**ファイル:** `us007-notifications.spec.ts`
**対応ユーザーストーリー:** US-007 (期限通知)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-060 | 通知ベルに未読数バッジ表示 | 期限切れタスク存在 | 1. ヘッダー確認 | 通知ベルにバッジが表示 |
| E2E-061 | 通知ベルクリックで通知一覧表示 | 通知存在 | 1. 通知ベルクリック | 通知一覧ページに遷移 |
| E2E-062 | 通知クリックで該当タスクに遷移 | 通知存在 | 1. 通知アイテムクリック | 該当タスクの詳細Sheetが表示 |
| E2E-063 | 「Mark all as read」で全既読化 | 未読通知存在 | 1. 「Mark all as read」クリック | バッジが消える、通知が既読化 |
| E2E-064 | 期限超過タスクのハイライト表示 | 期限切れタスク存在 | 1. タスク一覧確認 | 期限切れタスクが赤くハイライト |

---

### 3.8 US-009: フィルタリング・検索

**ファイル:** `us009-filtering-search.spec.ts`
**対応ユーザーストーリー:** US-009 (フィルタリング・検索)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-070 | ステータスフィルタ (TODO) | 複数ステータスのタスク存在 | 1. フィルタでTODO選択 | TODOタスクのみ表示 |
| E2E-071 | 優先度フィルタ (HIGH) | 複数優先度のタスク存在 | 1. フィルタでHIGH選択 | HIGHタスクのみ表示 |
| E2E-072 | タグフィルタ | タグ付きタスク存在 | 1. フィルタでタグ選択 | 該当タグのタスクのみ表示 |
| E2E-073 | 複合フィルタ (status + priority) | 複数タスク存在 | 1. TODO + HIGH で絞り込み | 両条件を満たすタスクのみ |
| E2E-074 | インクリメンタルサーチ | 複数タスク存在 | 1. 検索バーにキーワード入力 | 入力に応じて即座に結果が絞り込まれる |
| E2E-075 | 検索結果のハイライト表示 | 検索実行 | 1. キーワード入力 | 一致箇所がハイライトされる |
| E2E-076 | フィルタクリア | フィルタ適用中 | 1. フィルタクリア操作 | 全タスクが表示される |
| E2E-077 | 検索のdebounce動作 (300ms) | 検索バー表示 | 1. 素早く連続入力 | 最終入力から300ms後に検索実行 |
| E2E-078 | ガントチャートでのフィルタ反映 | ガントビュー + フィルタ | 1. フィルタ適用 2. ガントタブ確認 | フィルタされたタスクのバーのみ表示 |
| E2E-079 | カンバンでのフィルタ反映 | カンバンビュー + フィルタ | 1. フィルタ適用 2. カンバンタブ確認 | フィルタされたタスクのカードのみ表示 |

---

### 3.9 US-010: ダークモード切替

**ファイル:** `us010-dark-mode.spec.ts`
**対応ユーザーストーリー:** US-010 (ダークモード)

| TC-ID | テストケース | 前提条件 | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-080 | ダークモードトグルクリックで切替 | ライトモード表示中 | 1. ヘッダーのダークモードトグルクリック | 背景・テキストがダークモードに変更 |
| E2E-081 | ダークモードからライトモードに戻す | ダークモード表示中 | 1. トグルクリック | ライトモードに戻る |
| E2E-082 | ダークモード設定がlocalStorageに保存 | ダークモード切替 | 1. ダークモードに切替 2. ページリロード | ダークモードが維持される |
| E2E-083 | ガントチャートのダークモード対応 | ダークモード | 1. ガントチャート表示 | バー、今日線、矢印がダークモードで見える |
| E2E-084 | カンバンボードのダークモード対応 | ダークモード | 1. カンバンビュー表示 | カラム、カードがダークモードで見える |
| E2E-085 | ダイアログのダークモード対応 | ダークモード | 1. タスク作成ダイアログ表示 | ダイアログがダークモードに対応 |
| E2E-086 | システム設定連動 | システムダークモード | 1. OS設定をダークモードに | アプリがシステム設定に連動 |

---

## 4. スクリーンショット比較戦略

### 4.1 ベースライン管理

```
tests/e2e/screenshots/baseline/
  gantt-chart-light.png
  gantt-chart-dark.png
  kanban-board-light.png
  kanban-board-dark.png
  calendar-view-light.png
  task-detail-sheet.png
  login-page.png
  dashboard-empty.png
  dashboard-with-projects.png
```

### 4.2 比較設定

```typescript
// 各テストでのスクリーンショット比較
await expect(page).toHaveScreenshot("gantt-chart-light.png", {
  maxDiffPixelRatio: 0.01,  // 1%まで許容
  threshold: 0.2,            // ピクセルレベルの閾値
  animations: "disabled",    // アニメーション無効化
});
```

### 4.3 スクリーンショット対象画面

| 画面 | ライトモード | ダークモード | モバイル |
|------|-----------|-----------|---------|
| ログイン画面 | Yes | Yes | Yes |
| ダッシュボード（空） | Yes | Yes | Yes |
| ダッシュボード（データあり） | Yes | Yes | Yes |
| ガントチャート | Yes | Yes | No |
| カンバンボード | Yes | Yes | Yes |
| カレンダービュー | Yes | Yes | No |
| タスク詳細Sheet | Yes | Yes | Yes |
| プロジェクト設定 | Yes | Yes | Yes |
| 通知一覧 | Yes | Yes | Yes |

---

## 5. CI/CD統合

### 5.1 GitHub Actions ワークフロー

```yaml
# .github/workflows/e2e.yml
name: E2E Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: taskflow_e2e
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Setup database
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/taskflow_e2e

      - name: Build application
        run: npm run build

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run E2E tests
        run: npx playwright test
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/taskflow_e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: .test-logs/latest/e2e-report/
          retention-days: 30

      - name: Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: test-results/
          retention-days: 7
```

---

## 6. レスポンシブテスト

### 6.1 デバイス別テスト

| TC-ID | テストケース | デバイス | 手順 | 期待結果 |
|-------|------------|---------|------|---------|
| E2E-090 | モバイルでリストビュー優先表示 | iPhone 14 | 1. プロジェクト画面表示 | リストビューがデフォルト |
| E2E-091 | モバイルでガントチャート横スクロール | iPhone 14 | 1. ガントタブ選択 | 横スクロールで全体表示可能 |
| E2E-092 | タブレットでサイドバー折りたたみ | iPad | 1. 初期表示 | サイドバーが折りたたまれている |
| E2E-093 | デスクトップでフル表示 | Desktop Chrome | 1. 初期表示 | サイドバー + コンテンツが同時表示 |
| E2E-094 | タッチターゲット44px以上 | iPhone 14 | 1. ボタン計測 | タッチターゲットが44px以上 |

---

## 7. アクセシビリティテスト

| TC-ID | テストケース | 手順 | 期待結果 |
|-------|------------|------|---------|
| E2E-100 | キーボードナビゲーション | 1. Tabキーで全操作可能 | 全インタラクティブ要素にフォーカス可能 |
| E2E-101 | フォーカスインジケーター表示 | 1. Tabキー移動 | フォーカスされた要素が視覚的に識別可能 |
| E2E-102 | aria-label完備 | 1. axe-playwright で検証 | aria-label 未設定の警告が0 |
| E2E-103 | コントラスト比4.5:1以上 | 1. axe-playwright で検証 | コントラスト比違反が0 |
| E2E-104 | スクリーンリーダー対応 | 1. aria-roleが正しい | 構造が正しく読み上げられる |

---

## 8. テスト実行

### 8.1 コマンド

```bash
# Docker環境起動
docker compose -f docker-compose.e2e.yml up -d

# 全E2Eテスト実行
npx playwright test

# 特定シナリオ
npx playwright test tests/e2e/scenarios/us001-task-creation.spec.ts

# UIモード（インタラクティブ）
npx playwright test --ui

# 特定ブラウザ
npx playwright test --project=chromium

# スクリーンショット更新
npx playwright test --update-snapshots

# Docker環境停止
docker compose -f docker-compose.e2e.yml down -v
```

---

## 9. テスト数サマリー

| ユーザーストーリー | テストケース数 |
|-----------------|-------------|
| US-001: タスク作成フロー | 7 |
| US-002: ガントチャート表示・操作 | 8 |
| US-003: 依存関係設定 | 5 |
| US-004: ドラッグ&ドロップ操作 | 7 |
| US-005: 進捗率更新 | 6 |
| US-006: カンバンボード操作 | 7 |
| US-007: 通知確認 | 5 |
| US-009: フィルタリング・検索 | 10 |
| US-010: ダークモード切替 | 7 |
| レスポンシブテスト | 5 |
| アクセシビリティテスト | 5 |
| **合計** | **72** |

---

*Generated by CCAGI SDK Phase 2 - CodeGenAgent (Gen)*
